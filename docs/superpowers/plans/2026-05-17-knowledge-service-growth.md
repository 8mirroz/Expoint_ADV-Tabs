# Expoint ADV Knowledge-Driven Service Growth Plan

> Execution artifact for the active `executing-plans` rollout.

## Chosen route strategy

- Build `volumetric-letters` and `setup` as dedicated service pages.
- Keep `src/app/(marketing)/services/[slug]` as the generic fallback, not the primary growth path.
- Use `ServicePageData` and `components/service-pages/*` as the canonical pattern for rich B2B service routes.

## Batch order

1. Knowledge contracts and coverage map
2. Service page contract expansion
3. Runtime knowledge and LLM surfaces
4. SEO/AEO and sitemap completion
5. Quality gate hardening

## Current rollout note

- The worktree is already dirty.
- This rollout must avoid reverting unrelated user changes.
