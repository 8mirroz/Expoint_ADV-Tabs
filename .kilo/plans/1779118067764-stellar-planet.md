# План: Эволюция Sales Engine v2

## Текущее состояние
- ✅ Zustand store + facade реализованы
- ✅ Hero, Calculator, Checkout используют salesEngine
- ✅ Cart хранит reversible calculator metadata
- ✅ API routes для proposal и visualization созданы (стабы)
- ✅ Тесты покрывают основной flow (7 tests)

## Следующие шаги

### 1. Browser Smoke Testing
**Проблема:** Browser tool недоступен в текущем сеансе
**Решение:** 
- Добавить playwright/e2e тесты как альтернативу
- Создать простой ручной чек-лист для smoke

### 2. Capability Cards UI Primitive Integration
**Текущее:** CapabilityCard создан, но не интегрирован в Hero/Calculator/Checkout
**Задачи:**
- [ ] Заменить дублирующийся capability rendering в Hero.tsx на `<CapabilityCard>`
- [ ] Заменить в CalculatorContainer.tsx секцию "Sales draft"
- [ ] Заменить в CheckoutClient.tsx capability grid
- [ ] Удалить дублирующийся code после рефакторинга

### 3. Backend Endpoints Implementation
**Текущее:** API routes возвращают mock данные
**Задачи:**
- [ ] Интегрировать `/api/proposal/generate` с:
  - PDF generation (react-pdf или puppeteer)
  - Email delivery
  - CRM webhook
- [ ] Интегрировать `/api/visualization/request` с:
  - Очередь в базе (PostgreSQL/SQLite)
  - AI генерация (Midjourney API / DALL-E proxy)
  - Webhook уведомления

### 4. Split-Mode для Lead Submit
**Текущее:** `salesEngine.start()` отправляет lead сразу на `/api/lead`
**Задачи:**
- [ ] Добавить `saveDraftOnly: true` опцию в `start()`
- [ ] Создать `/api/draft/save` endpoint
- [ ] Добавить UI для "Save draft before submit"

### 5. Edge Cases & Validation
**Текущее:** Некоторые edge cases могут не обрабатываться
**Задачи:**
- [ ] Добавить валидацию для resume() когда cart пуст
- [ ] Обработка race conditions в saveQuoteCart
- [ ] Type guard для metadata в cart items
- [ ] Error boundaries в UI

## Приоритеты

| Приоритет | Задача | Ожидаемый результат |
|-----------|--------|-------------------|
| HIGH | Browser smoke tests | Уверенность в click-flow |
| HIGH | Integrate CapabilityCard | Убрать дублирование code (60% экономии) |
| MEDIUM | Backend integration | Реальные PDF и визуализации |
| LOW | Split-mode draft | Гибкость B2B процесса |

## Файлы для изменения

```
src/
├── components/
│   ├── sections/Hero.tsx (refactor capability section)
│   ├── calculator/CalculatorContainer.tsx (use CapabilityCard)
│   ├── checkout/CheckoutClient.tsx (use CapabilityCard)
│   └── ui/CapabilityCard.tsx (existing, ready)
├── lib/
│   └── salesEngine.ts (split-mode)
└── app/api/
    ├── proposal/route.ts (PDF integration)
    └── visualization/route.ts (AI queue)
```