# Skill: Zustand State Management

**Description**: High-performance, minimal state management for React.

## Patterns
- **Atomic Slices**: Divide complex stores into manageable slices.
- **Persistence**: Use `persist` middleware for data that must survive refreshes (e.g., unfinished configurator steps).
- **Selector Optimization**: Always use selectors to prevent unnecessary re-renders.
